#!/bin/sh
set -eu

# Restart running services so they pick up the new snap options
# (setup-env re-reads them on every start).
for service in semaphore.server semaphore.runner; do
  if snapctl services "$service" 2>/dev/null | grep -qw "active"; then
    snapctl restart "$service"
  fi
done
